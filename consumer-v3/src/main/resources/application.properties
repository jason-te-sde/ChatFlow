server.port=8081
rabbitmq.host=localhost
rabbitmq.port=5672
rabbitmq.username=guest
rabbitmq.password=guest

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/chatflow?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&rewriteBatchedStatements=true
spring.datasource.username=chatflow
spring.datasource.password=chatflow123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection pool (HikariCP default)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# Batch write tuning ? optimal found through testing
db.batch.size=500
db.batch.flush-ms=500
db.writer.threads=4